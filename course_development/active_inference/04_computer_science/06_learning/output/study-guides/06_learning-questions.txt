Study Questions: Learning

Why does Active Inference use Dirichlet distributions to represent uncertainty about the A and B matrices?

Write the formula for the expected A-matrix $\mathbb{E}[\mathbf{A}]$ given concentration parameters $\mathbf{p}A$. What does expectedA(pA) compute?

If pA = np.ones((3, 2)) (all concentrations = 1), what is the expected A-matrix? What does this represent in terms of prior beliefs?

Explain the update rule for updatedirichletA(). Which entries of pA increase after observing $o = 0$ with beliefs $q(s) = [0.7, 0.3]$?

How does the learning rate $\eta$ affect the pA update? What happens if $\eta = 0$? If $\eta = 10$?

Write the complete online learning loop (perception, action, pA update, pB update) for 50 timesteps.

After 100 learning updates, how would you measure whether the agent has accurately recovered the true A-matrix?

What is expectedB(pB) and how is it used after each pB update?

Compute updatedirichletB() by hand for $q(s) = [1, 0]$, $q(s') = [0, 1]$, $a = 0$, $\eta = 1$. Which entry of $\mathbf{p}B$ changes?

What does dirichletentropy(alpha) measure? Is a low or high Dirichlet entropy desirable for a well-learned model?

How does the agent's learned A-matrix affect its state inference? Describe the feedback loop between learning and perception.

What is Bayesian Model Reduction and what problem does it solve?

When does bayesianmodelreduction(pAfull, pAreduced) return a negative ΔF? What does this mean for model selection?

How would you visualize the convergence of pA toward the true A? Which visualization function would you use?

Design an experiment with 3 episodes of 50 steps each. Between episodes, reset the environment but keep the accumulated pA. Predict how performance changes across episodes.

What is updatedirichletD() and when would you use it? How does it differ from updating pA?

If the true A-matrix changes after 50 steps (non-stationary environment), how would you modify the learning to adapt? Consider the effect of learning rate.

What is the relationship between the concentration magnitudes in pA and the agent's confidence in its likelihood model?

Use plotdirichlet_concentration() to compare the initial and learned pA. What does the visualization show?

Explain why Dirichlet learning is online (incremental) rather than batch. What are the computational advantages?