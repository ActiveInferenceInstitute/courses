Lab 01: Building and Exploring a Discrete Environment

Objective

Build a DiscreteEnvironment from scratch, step through it, record trajectories, and visualize the generative process.

Prerequisites

Python with NumPy and matplotlib installed
Familiarity with probability distributions and matrix notation
Access to the activeinference library (src/activeinference/)

Part 1: Constructing the Environment

Goal: Create a 3-state, 2-observation environment with 2 actions.

Define a trueA matrix of shape (2, 3) where:
   - State 0 produces observation 0 with probability 0.8
   - State 1 produces each observation with equal probability
   - State 2 produces observation 1 with probability 0.9

Define a trueB tensor of shape (3, 3, 2) where:
   - Action 0 is the identity (stay in current state)
   - Action 1 rotates: state 0 → 1, state 1 → 2, state 2 → 0

Create a DiscreteEnvironment starting in state 0.

Response: 
<div style="border: 1px solid #ccc; border-radius: 4px; min-height: 96px; padding: 8px; margin: 8px 0; background-color: #fafafa;"><em style="color: #999; font-size: 0.85em;">Write your response here</em></div>

Part 2: Running a Trajectory

Goal: Step the environment for 20 timesteps using a fixed action sequence.

Reset the environment to state 0.
Alternate between action 0 (stay) and action 1 (rotate) for 20 steps.
Record the state, observation, and action at each step.
Print the full trajectory.

Response: 
<div style="border: 1px solid #ccc; border-radius: 4px; min-height: 96px; padding: 8px; margin: 8px 0; background-color: #fafafa;"><em style="color: #999; font-size: 0.85em;">Write your response here</em></div>

Part 3: Visualizing the Trajectory

Goal: Use plotenvironmenttrajectory to visualize the state-observation-action sequence.

After running Part 2, extract env.history.
Call plotenvironmenttrajectory to generate a figure.
Save the figure to output/lab01trajectory.png.

Response: 
<div style="border: 1px solid #ccc; border-radius: 4px; min-height: 96px; padding: 8px; margin: 8px 0; background-color: #fafafa;"><em style="color: #999; font-size: 0.85em;">Write your response here</em></div>

Part 4: Empirical Likelihood Estimation

Goal: Verify the trueA matrix by collecting observations.

Reset the environment. Fix the state by using action 0 (identity) from a known starting state.
Collect 1000 observations from each state (reset to each state, step with identity action 1000 times).
Compute the empirical frequency of each observation per state.
Compare with the original trueA matrix.

Response: 
<div style="border: 1px solid #ccc; border-radius: 4px; min-height: 96px; padding: 8px; margin: 8px 0; background-color: #fafafa;"><em style="color: #999; font-size: 0.85em;">Write your response here</em></div>

Part 5: Analysis Questions

When you ran 20 steps with alternating actions, how many unique states did the trajectory visit? Was this predictable from the trueB matrix?

Did your empirical likelihood estimates in Part 4 converge to the true values? How many samples were needed for 2-decimal accuracy?

What would change if you made trueA = np.eye(3, 2).T (a non-square identity-like matrix)? Would the environment still be valid?

Response: 
<div style="border: 1px solid #ccc; border-radius: 4px; min-height: 96px; padding: 8px; margin: 8px 0; background-color: #fafafa;"><em style="color: #999; font-size: 0.85em;">Write your response here</em></div>

Summary

| Skill | Library Component | Status |
|-------|------------------|--------|
| Create environments with custom A and B matrices | DiscreteEnvironment(trueA, trueB) | |
| Step through environments and collect trajectories | env.step(action), env.history | |
| Visualize state-observation trajectories | plotenvironmenttrajectory() | |
| Empirically verify likelihood matrices | Manual frequency counting vs trueA | |
| Understand the generative process abstraction | Generative process ≠ generative model | |