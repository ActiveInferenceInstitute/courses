# 質問：システム

1. Active Inferenceにおける**生成プロセス**と**生成モデル**の違いとは何か、そして`active_inference`ライブラリ内のどのクラスがそれぞれを実装しているか？

2. `true_A`行列（形状：(3, 4)）が与えられた場合、環境は観察モダリティと隠れ状態のそれぞれいくつ持っているか？

3. `true_A = np.eye(3)`の場合、これは環境の観察ノイズについて何を意味するのか？ `env.step(action)`は常にどのような値を返すか？

4. 2つの状態、2つの観察システムが状態1から始まる`DiscreteEnvironment`を作成するコードを記述してください。

5. `true_A`の列がそれぞれ合計で1.0である理由を説明してください。各列はどのような確率分布を表しているか？

6. `true_B`の形状が`(4, 4, 3)`の場合、環境はサポートする隠れ状態とアクションの数はそれぞれいくつですか？

7. `env.step(action=0)`を呼び出す際に、内部的に何が起こるか説明してください。2つのサンプリングステップを順に記述してください。

8. `$T$`ステップ後に、`env.history["states"]`が`env.history["actions"]`よりも1つ多い理由は何ですか？

9. アクション0がアイデンティティ、アクション1が周期的な順序付け ($0 \to 1 \to 2 \to 0$) である3つの状態の環境に対して、`true_B`行列を構築してください。

10. `env.get_info()`が何を返すか、シミュレーションループ中にそれを使用する状況を説明してください。

11. 2次元の`true_B`行列（形状：(N, N)）を`DiscreteEnvironment`に渡した場合、環境はいくつものアクションを持つか？ どのような暗黙的な仮定がありますか？

12. `DiscreteEnvironment.__init__`が`true_A`と`true_B`に対して行う検証チェックを説明してください。チェックが失敗した場合にどのようなエラーが raise されますか？

13. `env.reset()`によって生成される初期観察が、初期状態を決定的に指定した場合でも確率的である理由は何ですか？

14. 観察0と1が状態0と1の間で曖昧であり、観察2が状態2をユニークに識別する、部分的に観測可能な3つの状態システムに対して、`true_A`行列を設計してください。

15. 「滑りやすい床」環境をシミュレートする方法を説明してください。意図したアクションが80%の確率で成功し、ランダムな隣接状態に20%の確率で遷移する場合です。

16. コードで、`DiscreteEnvironment`によって生成された状態と観察の軌跡を視覚化する方法を説明してください。どの視覚化関数を使用しますか？

17. 環境内の`true_A`行列とエージェントの`GenerativeModel`内の`A`行列の関係とは何ですか。どのような条件下で自由エネルギーが最小化されますか？

18. `env.step(action)`を100回実行し、観察を収集した場合、データから`true_A`行列を経験的に推定する方法を説明してください。

19. `env.timestep`が`reset()`と`step()`中にどのように更新されるか説明してください。`timestep`と`len(env.history["actions"])`に関連付けられた不変量は何ですか？

20. 生成プロセスが5つの隠れ状態を持ち、エージェントの生成モデルが3つしか持たないシナリオを説明してください。信念更新に対する計算上の影響は何ですか？